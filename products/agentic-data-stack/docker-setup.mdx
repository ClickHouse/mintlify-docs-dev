---
sidebarTitle: 'Docker setup'
title: 'Set up the Agentic Data Stack with Docker Compose'
description: 'Run the full agentic data stack — ClickHouse, LibreChat, the MCP server, and Langfuse — via Docker Compose'
doc_type: 'guide'
keywords: ['agentic data stack', 'docker compose', 'setup', 'LibreChat', 'MCP', 'Langfuse']
---

{/* STUB — DOC-734. Watch the SF meetup demo video before drafting (per DOC-732). */}

<Note>
**Draft stub.** Scaffolded for navigation. The integrated Docker Compose setup guide is pending (DOC-734).
</Note>

This guide will walk through standing up the complete bundle with Docker Compose: ClickHouse, the LibreChat front-end, the ClickHouse MCP server, and Langfuse for tracing — wired together so you can talk to your data out of the box.

{/* TODO:
  - prerequisites (Docker)
  - the compose file / bundle repo
  - bring up the stack
  - first prompt against sample data
  - point to each component page for detail
  Source: SF meetup demo. */}
