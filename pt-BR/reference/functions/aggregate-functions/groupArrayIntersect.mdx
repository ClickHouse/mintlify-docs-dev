---
description: 'Retorna a interseção dos arrays fornecidos (retorna todos os itens dos arrays
  que estão presentes em todos os arrays fornecidos).'
slug: /sql-reference/aggregate-functions/grouparrayintersect
title: 'groupArrayIntersect'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="groupArrayIntersect">
  ## groupArrayIntersect
</div>

Introduzido em: v24.2.0

Retorna a interseção dos arrays fornecidos (retorna todos os itens dos arrays que estão presentes em todos eles).

**Sintaxe**

```sql
groupArrayIntersect(x)
```

**Argumentos**

* `x` — Argumento (nome da coluna ou expressão). [`Any`](/pt-BR/reference/data-types)

**Valor retornado**

Retorna um array contendo os elementos presentes em todos os arrays. [`Array`](/pt-BR/reference/data-types/array)

**Exemplos**

**Exemplo de uso**

```sql title=Query
-- Criar tabela com engine Memory
CREATE TABLE numbers (
    a Array(Int32)
) ENGINE = Memory;

-- Inserir dados de exemplo
INSERT INTO numbers VALUES
    ([1,2,4]),
    ([1,5,2,8,-1,0]),
    ([1,5,7,5,8,2]);

SELECT groupArrayIntersect(a) AS intersection FROM numbers;
```

```response title=Response
┌─intersection──────┐
│ [1, 2]            │
└───────────────────┘
```

{/*AUTOGENERATED_END*/}