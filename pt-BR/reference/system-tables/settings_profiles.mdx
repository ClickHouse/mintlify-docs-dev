---
description: 'Tabela de sistema que contém propriedades dos perfis de configuração definidos.'
keywords: ['tabela de sistema', 'settings_profiles']
slug: /operations/system-tables/settings_profiles
title: 'system.settings_profiles'
doc_type: 'reference'
---

<div id="description">
  ## Descrição
</div>

Contém as propriedades dos perfis de configuração.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/pt-BR/reference/data-types)) — Nome do perfil de configuração.
* `id` ([UUID](/pt-BR/reference/data-types)) — ID do perfil de configuração.
* `storage` ([String](/pt-BR/reference/data-types)) — Caminho para o armazenamento dos perfis de configuração. Configurado no parâmetro `access_control_path`.
* `num_elements` ([UInt64](/pt-BR/reference/data-types)) — Número de elementos deste perfil na tabela `system.settings_profile_elements`.
* `apply_to_all` ([UInt8](/pt-BR/reference/data-types)) — Indica que o perfil de configuração se aplica a todos os roles e/ou usuários.
* `apply_to_list` ([Array(String)](/pt-BR/reference/data-types)) — Lista dos roles e/ou usuários aos quais o perfil de configuração se aplica.
* `apply_to_except` ([Array(String)](/pt-BR/reference/data-types)) — O perfil de configuração se aplica a todos os roles e/ou usuários, exceto aos listados.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## Veja também
</div>

* [SHOW PROFILES](/pt-BR/reference/statements/show#show-profiles)