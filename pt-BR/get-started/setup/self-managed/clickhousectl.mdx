---
description: 'Instale e gerencie versões locais do ClickHouse usando o clickhousectl, o ClickHouse CLI'
keywords: ['ClickHouse', 'instalar', 'clickhousectl', 'CLI']
sidebarTitle: 'clickhousectl (CLI)'
slug: /install/clickhousectl
title: 'Instale com o ClickHouse CLI'
doc_type: 'guide'
---

<Info>
  **Usuários do Windows**

  O ClickHouse é executado nativamente no Linux e no macOS. No Windows, execute estas etapas no
  [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/about).
</Info>

O ClickHouse CLI (`clickhousectl`) ajuda você a instalar e gerenciar versões locais do ClickHouse, iniciar servidores e executar consultas.

<Steps>
  <Step>
    ## Instale o ClickHouse CLI

    ```bash
    curl https://clickhouse.com/cli | sh
    ```

    Um alias `chctl` também é criado automaticamente para facilitar.
  </Step>

  <Step>
    ## Instale o ClickHouse

    Instale a versão estável mais recente do ClickHouse:

    ```bash
    clickhousectl local install stable
    ```

    Você também pode instalar uma versão específica:

    ```bash
    clickhousectl local install lts             # Versão LTS mais recente
    clickhousectl local install 25.6            # Versão 25.6.x.x mais recente
    clickhousectl local install 25.6.1.1        # Versão exata
    ```
  </Step>

  <Step>
    ## Inicie o clickhouse-server

    ```bash
    clickhousectl local server start
    ```

    O servidor é executado em segundo plano. Para verificar se ele está em execução:

    ```bash
    clickhousectl local server list
    ```
  </Step>

  <Step>
    ## Inicie o clickhouse-client

    ```bash
    clickhousectl local client
    ```

    Você verá algo parecido com isto:

    ```response
    ClickHouse client version 24.5.1.117 (official build).
    Connecting to localhost:9000 as user default.
    Connected to ClickHouse server version 24.5.1.

    local-host :)
    ```

    Agora você já pode começar a enviar comandos SQL para o ClickHouse!

    <Tip>
      O [Quick Start](/pt-BR/get-started/setup/install) orienta você nas etapas para criar tabelas e inserir dados.
    </Tip>
  </Step>
</Steps>
