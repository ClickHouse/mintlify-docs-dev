---
description: '이 테이블에는 즉시 계산되어 Prometheus 포맷으로 내보낼 수 있는 히스토그램 메트릭이 포함되어 있습니다. 이 테이블은 항상 최신 상태입니다.'
keywords: ['시스템 테이블', 'histogram_metrics']
slug: /operations/system-tables/histogram_metrics
title: 'system.histogram_metrics'
doc_type: '참고'
---

import SystemTableCloud from '/snippets/ko/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## 설명
</div>

이 테이블(table)에는 즉시 계산되어 Prometheus 포맷으로 내보낼 수 있는 히스토그램 메트릭이 포함되어 있습니다. 항상 최신 상태입니다. 더 이상 사용이 권장되지 않는 `system.latency_log`를 대체합니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `metric` ([String](/ko/reference/data-types)) — 메트릭 이름.
* `value` ([Float64](/ko/reference/data-types)) — 메트릭 값.
* `description` ([String](/ko/reference/data-types)) — 메트릭 설명.
* `labels` ([Map(String, String)](/ko/reference/data-types)) — 메트릭 레이블.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## 예시
</div>

다음과 같은 쿼리를 사용하면 모든 히스토그램 메트릭을 Prometheus 포맷으로 내보낼 수 있습니다.

```sql
SELECT
  metric AS name,
  toFloat64(value) AS value,
  description AS help,
  labels,
  'histogram' AS type
FROM system.histogram_metrics
FORMAT Prometheus
```

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)를 나타내는 메트릭으로, 비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 활성 소켓마다 1회씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 각 활성 소켓별로 1회씩 측정되는 HTTP 연결 풀 소켓의 Kernel TCP 버퍼 메모리(`sk_rmem_alloc / sk_wmem_alloc`)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 각 활성 소켓별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 각 활성 소켓에 대해 1회 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓에서 사용되는 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)로, 비동기 메트릭을 스크레이프할 때마다 활성 소켓마다 1회씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 상태의 각 소켓별로 한 번 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(`sk_rmem_alloc` / `sk_wmem_alloc`)를 나타내는 메트릭으로, 비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓에 대해 한 번씩 관찰됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓에 대해 한 번씩 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓마다 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며 `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓별로 한 번씩 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 1회 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(`sk_rmem_alloc / sk_wmem_alloc`)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 활성 상태인 각 소켓에서 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓별로 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)로, 비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 현재 활성 상태인 각 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)로, 비동기 메트릭이 스크레이프될 때마다 활성 상태인 각 소켓에 대해 1회씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓에 대해 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(`sk_rmem_alloc / sk_wmem_alloc`)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 비동기 메트릭이 스크레이프될 때마다 활성 소켓별로 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓별로 1회 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(`sk_rmem_alloc / sk_wmem_alloc`)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 활성 상태인 소켓(live socket)마다 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(`sk_rmem_alloc` / `sk_wmem_alloc`)로, 비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 활성 상태인 각 소켓에 대해 1회씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 비동기 메트릭을 스크레이프할 때마다 활성 소켓당 1회 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 1회씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 1회씩 관찰되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 활성 소켓마다 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 비동기 메트릭이 스크레이프될 때마다 활성 소켓별로 1회씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)로, 비동기 메트릭을 스크레이프할 때마다 활성 소켓마다 한 번씩 관찰됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓에 대해 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이고, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)로, 비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 비동기 메트릭이 스크레이프될 때마다 활성 소켓별로 한 번씩 측정됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에서 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

비동기 메트릭이 스크레이프될 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes">
  ### http_pool_tcp_buf_bytes
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)로, 비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓에 대해 한 번씩 측정됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes_sum">
  ### http_pool_tcp_buf_bytes_sum
</div>

HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)를 나타냅니다. 비동기 메트릭을 스크레이프할 때마다 활성 상태인 각 소켓에 대해 한 번씩 관측됩니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes_sum">
  ### http_pool_tcp_buf_bytes_sum
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes_sum">
  ### http_pool_tcp_buf_bytes_sum
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes_sum">
  ### http_pool_tcp_buf_bytes_sum
</div>

비동기 메트릭이 스크레이프될 때마다 활성 소켓당 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes_sum">
  ### http_pool_tcp_buf_bytes_sum
</div>

비동기 메트릭을 스크레이프할 때마다 활성 소켓별로 한 번씩 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="http_pool_tcp_buf_bytes_sum">
  ### http_pool_tcp_buf_bytes_sum
</div>

비동기 메트릭을 스크레이프할 때마다 현재 활성 상태인 각 소켓(live socket)별로 1회 관측되는 HTTP 연결 풀 소켓의 커널 TCP 버퍼 메모리(sk&#95;rmem&#95;alloc / sk&#95;wmem&#95;alloc)입니다. 레이블: `group`은 `disk`, `storage`, `http` 중 하나이며, `direction`은 `rcv` 또는 `snd`입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트 단위)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치의 크기(바이트)입니다.

<div id="keeper_batch_size_bytes">
  ### keeper_batch_size_bytes
</div>

Raft로 전송되는 배치 크기(바이트)입니다.

<div id="keeper_batch_size_bytes_sum">
  ### keeper_batch_size_bytes_sum
</div>

Raft로 전송되는 배치 크기(바이트)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기를 요소 수로 나타낸 값입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기(요소 기준)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기를 요소 수로 나타낸 값입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기(요소 수)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기(요소 수)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기(요소 수)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기(요소 수)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치 크기(요소 수 기준)입니다.

<div id="keeper_batch_size_elements">
  ### keeper_batch_size_elements
</div>

Raft로 전송되는 배치의 크기(요소 수)입니다.

<div id="keeper_batch_size_elements_sum">
  ### keeper_batch_size_elements_sum
</div>

Raft로 전송된 배치의 크기(요소 수)입니다.

<div id="keeper_client_queue_duration_milliseconds">
  ### keeper_client_queue_duration_milliseconds
</div>

요청이 큐에 추가되기까지 대기하고, 처리되기 전에 큐에서 대기하는 시간

<div id="keeper_client_queue_duration_milliseconds">
  ### keeper_client_queue_duration_milliseconds
</div>

요청이 큐에 추가되기까지 기다리는 시간과 처리되기 전까지 큐에서 대기하는 시간

<div id="keeper_client_queue_duration_milliseconds">
  ### keeper_client_queue_duration_milliseconds
</div>

요청이 큐에 추가되기를 기다리는 시간과 처리되기 전까지 큐에서 대기하는 시간

<div id="keeper_client_queue_duration_milliseconds">
  ### keeper_client_queue_duration_milliseconds
</div>

요청이 큐에 추가되기까지 기다리는 시간과 처리되기 전 큐에서 대기하는 시간

<div id="keeper_client_queue_duration_milliseconds">
  ### keeper_client_queue_duration_milliseconds
</div>

요청이 큐에 추가되기까지 대기하는 시간과 처리되기 전까지 큐에서 대기하는 시간

<div id="keeper_client_queue_duration_milliseconds_sum">
  ### keeper_client_queue_duration_milliseconds_sum
</div>

요청이 큐에 들어가기까지 대기하고, 처리되기 전까지 큐에서 대기하는 데 걸린 시간

<div id="keeper_dispatcher_requests_queue_time_milliseconds">
  ### keeper_dispatcher_requests_queue_time_milliseconds
</div>

요청이 디스패처 요청 큐에서 대기한 시간

<div id="keeper_dispatcher_requests_queue_time_milliseconds">
  ### keeper_dispatcher_requests_queue_time_milliseconds
</div>

디스패처 requests 큐에서 요청이 대기하는 시간

<div id="keeper_dispatcher_requests_queue_time_milliseconds">
  ### keeper_dispatcher_requests_queue_time_milliseconds
</div>

요청이 디스패처 requests 큐에서 대기한 시간

<div id="keeper_dispatcher_requests_queue_time_milliseconds">
  ### keeper_dispatcher_requests_queue_time_milliseconds
</div>

요청이 디스패처 요청 큐에서 대기하는 시간

<div id="keeper_dispatcher_requests_queue_time_milliseconds">
  ### keeper_dispatcher_requests_queue_time_milliseconds
</div>

디스패처 요청 큐에서 요청이 대기하는 시간

<div id="keeper_dispatcher_requests_queue_time_milliseconds_sum">
  ### keeper_dispatcher_requests_queue_time_milliseconds_sum
</div>

요청이 디스패처 요청 큐에서 대기하는 데 걸리는 시간

<div id="keeper_dispatcher_responses_queue_time_milliseconds">
  ### keeper_dispatcher_responses_queue_time_milliseconds
</div>

응답이 디스패처 응답 큐에서 대기하는 시간

<div id="keeper_dispatcher_responses_queue_time_milliseconds">
  ### keeper_dispatcher_responses_queue_time_milliseconds
</div>

응답이 디스패처 응답 큐에서 대기하는 시간

<div id="keeper_dispatcher_responses_queue_time_milliseconds">
  ### keeper_dispatcher_responses_queue_time_milliseconds
</div>

응답이 디스패처 응답 큐에서 대기하는 시간

<div id="keeper_dispatcher_responses_queue_time_milliseconds">
  ### keeper_dispatcher_responses_queue_time_milliseconds
</div>

응답이 디스패처 응답 큐에서 대기하는 시간

<div id="keeper_dispatcher_responses_queue_time_milliseconds">
  ### keeper_dispatcher_responses_queue_time_milliseconds
</div>

응답이 디스패처 응답 큐에서 대기하는 시간

<div id="keeper_dispatcher_responses_queue_time_milliseconds_sum">
  ### keeper_dispatcher_responses_queue_time_milliseconds_sum
</div>

응답이 디스패처 응답 큐에서 대기하는 시간

<div id="keeper_read_process_time_milliseconds">
  ### keeper_read_process_time_milliseconds
</div>

읽기 요청 처리에 걸리는 시간

<div id="keeper_read_process_time_milliseconds">
  ### keeper_read_process_time_milliseconds
</div>

읽기 요청을 처리하는 시간

<div id="keeper_read_process_time_milliseconds">
  ### keeper_read_process_time_milliseconds
</div>

읽기 요청 처리 시간

<div id="keeper_read_process_time_milliseconds">
  ### keeper_read_process_time_milliseconds
</div>

읽기 요청 처리에 걸리는 시간

<div id="keeper_read_process_time_milliseconds">
  ### keeper_read_process_time_milliseconds
</div>

읽기 요청 처리 시간

<div id="keeper_read_process_time_milliseconds_sum">
  ### keeper_read_process_time_milliseconds_sum
</div>

읽기 요청 처리 시간

<div id="keeper_read_wait_for_write_time_milliseconds">
  ### keeper_read_wait_for_write_time_milliseconds
</div>

읽기 요청가 의존하는 쓰기 요청가 완료될 때까지의 대기 시간

<div id="keeper_read_wait_for_write_time_milliseconds">
  ### keeper_read_wait_for_write_time_milliseconds
</div>

읽기 요청가 의존하는 쓰기 요청의 완료를 기다리는 시간

<div id="keeper_read_wait_for_write_time_milliseconds">
  ### keeper_read_wait_for_write_time_milliseconds
</div>

읽기 요청이 의존하는 쓰기 요청이 완료될 때까지 대기하는 시간

<div id="keeper_read_wait_for_write_time_milliseconds">
  ### keeper_read_wait_for_write_time_milliseconds
</div>

읽기 요청가 의존하는 쓰기 요청가 완료될 때까지 기다린 시간

<div id="keeper_read_wait_for_write_time_milliseconds">
  ### keeper_read_wait_for_write_time_milliseconds
</div>

read request가 의존하는 쓰기 요청가 완료될 때까지의 대기 시간

<div id="keeper_read_wait_for_write_time_milliseconds_sum">
  ### keeper_read_wait_for_write_time_milliseconds_sum
</div>

읽기 요청이 의존하는 쓰기 요청의 완료를 기다리는 시간

<div id="keeper_receive_request_time_milliseconds">
  ### keeper_receive_request_time_milliseconds
</div>

TCP handler에서 클라이언트 요청을 수신하고 파싱하는 데 걸리는 시간

<div id="keeper_receive_request_time_milliseconds">
  ### keeper_receive_request_time_milliseconds
</div>

TCP handler에서 클라이언트 요청을 수신하고 파싱하는 데 걸리는 시간

<div id="keeper_receive_request_time_milliseconds">
  ### keeper_receive_request_time_milliseconds
</div>

TCP handler에서 클라이언트의 요청을 수신하고 파싱하는 데 걸리는 시간

<div id="keeper_receive_request_time_milliseconds">
  ### keeper_receive_request_time_milliseconds
</div>

TCP handler에서 클라이언트 요청을 수신하고 파싱하는 데 걸리는 시간

<div id="keeper_receive_request_time_milliseconds">
  ### keeper_receive_request_time_milliseconds
</div>

TCP handler가 클라이언트 요청을 수신하고 파싱하는 데 걸리는 시간

<div id="keeper_receive_request_time_milliseconds_sum">
  ### keeper_receive_request_time_milliseconds_sum
</div>

TCP handler에서 클라이언트 요청을 수신하고 파싱하는 데 걸리는 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초 단위)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(단위: 밀리초)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초(ms) 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초 단위)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초 단위)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간입니다.

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(밀리초 단위)

<div id="keeper_response_time_ms">
  ### keeper_response_time_ms
</div>

Keeper의 응답 시간(단위: 밀리초)

<div id="keeper_response_time_ms_sum">
  ### keeper_response_time_ms_sum
</div>

Keeper의 응답 시간(단위: 밀리초)

<div id="keeper_response_time_ms_sum">
  ### keeper_response_time_ms_sum
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_response_time_ms_sum">
  ### keeper_response_time_ms_sum
</div>

밀리초 단위의 Keeper 응답 시간

<div id="keeper_send_response_time_milliseconds">
  ### keeper_send_response_time_milliseconds
</div>

TCP handler에서 클라이언트로 응답을 전송하는 데 걸리는 시간(큐 대기 및 소켓 쓰기 포함)

<div id="keeper_send_response_time_milliseconds">
  ### keeper_send_response_time_milliseconds
</div>

TCP handler에서 클라이언트에 응답을 보내는 데 걸리는 시간(큐 대기 및 소켓 쓰기 포함)

<div id="keeper_send_response_time_milliseconds">
  ### keeper_send_response_time_milliseconds
</div>

TCP handler에서 클라이언트로 응답을 전송하는 데 걸리는 시간(큐 대기 및 소켓 쓰기 포함)

<div id="keeper_send_response_time_milliseconds">
  ### keeper_send_response_time_milliseconds
</div>

TCP handler에서 클라이언트로 응답을 전송하는 시간(큐 대기 및 소켓 쓰기 포함)

<div id="keeper_send_response_time_milliseconds">
  ### keeper_send_response_time_milliseconds
</div>

TCP handler에서 클라이언트로 응답을 전송하는 데 걸리는 시간(큐 대기 및 소켓 쓰기 포함)

<div id="keeper_send_response_time_milliseconds_sum">
  ### keeper_send_response_time_milliseconds_sum
</div>

TCP handler에서 클라이언트로 응답을 전송하는 데 걸리는 시간(큐 대기 및 소켓 쓰기 포함)

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 후 쓰기 요청을 처리하는 데 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 후 쓰기 요청 처리에 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 이후 쓰기 요청 처리에 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 이후 쓰기 요청을 처리하는 데 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 이후 쓰기 요청을 처리하는 데 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 이후 쓰기 요청을 처리하는 데 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 후 쓰기 요청 처리에 걸리는 시간

<div id="keeper_write_commit_time_milliseconds">
  ### keeper_write_commit_time_milliseconds
</div>

Raft 커밋 후 쓰기 요청을 처리하는 데 걸리는 시간

<div id="keeper_write_commit_time_milliseconds_sum">
  ### keeper_write_commit_time_milliseconds_sum
</div>

Raft 커밋 이후 쓰기 요청을 처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds">
  ### keeper_write_pre_commit_time_milliseconds
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="keeper_write_pre_commit_time_milliseconds_sum">
  ### keeper_write_pre_commit_time_milliseconds_sum
</div>

Raft 커밋 전에 쓰기 요청을 전처리하는 데 걸리는 시간

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

각 S3 읽기 요청 connection에서 읽은 바이트 수.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수입니다.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

각 S3 읽기 요청 연결에서 읽은 바이트 수입니다.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수입니다.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 connection당 읽은 바이트 수.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수입니다.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수입니다.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수입니다.

<div id="s3_read_request_bytes">
  ### s3_read_request_bytes
</div>

S3 읽기 요청 연결당 읽은 바이트 수입니다.

<div id="s3_read_request_bytes_sum">
  ### s3_read_request_bytes_sum
</div>

S3 읽기 요청 connection당 읽은 바이트 수입니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

요청 시작부터 연결 종료까지의 S3 읽기 요청 연결 지속 시간이며, 단위는 마이크로초입니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

요청 시작부터 연결 종료까지의 S3 읽기 요청 연결 지속 시간으로, 단위는 마이크로초입니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

S3 읽기 요청 연결의 지속 시간입니다. 요청 시작부터 연결 종료까지의 시간을 마이크로초 단위로 측정합니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

S3 읽기 요청 연결의 지속 시간으로, 요청 시작부터 연결 종료까지를 마이크로초 단위로 나타냅니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

요청 시작부터 연결 종료까지의 S3 읽기 요청 연결 지속 시간을 마이크로초 단위로 나타냅니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

S3 읽기 요청 연결의 지속 시간입니다. 요청 시작부터 연결 종료까지를 마이크로초 단위로 측정합니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

요청 시작부터 연결 종료까지의 S3 읽기 요청 연결 지속 시간이며, 단위는 마이크로초입니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

요청 시작부터 연결 종료까지의 S3 읽기 요청 연결 지속 시간(Duration)을 마이크로초 단위로 나타냅니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

S3 읽기 요청 연결의 소요 시간으로, 요청 시작부터 연결 종료까지를 마이크로초 단위로 나타냅니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

S3 읽기 요청 연결의 지속 시간입니다. 요청 시작부터 연결 종료까지를 마이크로초 단위로 측정합니다.

<div id="s3_read_request_duration_microseconds">
  ### s3_read_request_duration_microseconds
</div>

S3 읽기 요청 연결의 지속 시간으로, 요청 시작부터 연결 종료까지를 마이크로초 단위로 나타냅니다.

<div id="s3_read_request_duration_microseconds_sum">
  ### s3_read_request_duration_microseconds_sum
</div>

요청 시작부터 연결 종료까지의 S3 읽기 요청 연결 지속 시간이며, 단위는 마이크로초입니다.