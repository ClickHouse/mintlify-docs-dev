---
description: '특정 테이블에 대한 필터와 이 ROW POLICY를 사용해야 하는 역할 및/또는 사용자 목록이 포함된 시스템 테이블(system table)입니다.'
keywords: ['system table', 'row_policies']
slug: /operations/system-tables/row_policies
title: 'system.row_policies'
doc_type: 'reference'
---

<div id="description">
  ## 설명
</div>

특정 테이블에 대한 필터와, 이 ROW POLICY를 사용해야 하는 역할 및/또는 사용자 목록을 포함합니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ko/reference/data-types)) — ROW POLICY의 이름입니다.
* `short_name` ([String](/ko/reference/data-types)) — ROW POLICY의 축약 이름입니다. ROW POLICY 이름은 복합적으로 구성됩니다. 예를 들어 `myfilter ON mydb.mytable`에서 `myfilter ON mydb.mytable`는 ROW POLICY의 이름이고, `myfilter`는 축약 이름입니다.
* `database` ([String](/ko/reference/data-types)) — 데이터베이스 이름입니다.
* `table` ([String](/ko/reference/data-types)) — 테이블 이름입니다. 데이터베이스에 대한 정책이면 비어 있습니다.
* `id` ([UUID](/ko/reference/data-types)) — ROW POLICY ID입니다.
* `storage` ([String](/ko/reference/data-types)) — ROW POLICY이 저장된 디렉터리의 이름입니다.
* `select_filter` ([Nullable(String)](/ko/reference/data-types)) — SELECT 쿼리에서 필터링에 사용되는 표현식입니다.
* `is_restrictive` ([UInt8](/ko/reference/data-types)) — ROW POLICY이 행에 대한 접근을 제한하는지 여부를 나타냅니다. 값: • 0 — ROW POLICY이 `AS PERMISSIVE` 절로 정의됩니다. • 1 — ROW POLICY이 `AS RESTRICTIVE` 절로 정의됩니다.
* `apply_to_all` ([UInt8](/ko/reference/data-types)) — 모든 역할 및/또는 사용자에 대해 ROW POLICY이 설정되어 있음을 나타냅니다.
* `apply_to_list` ([Array(String)](/ko/reference/data-types)) — ROW POLICY이 적용되는 역할 및/또는 사용자의 목록입니다.
* `apply_to_except` ([Array(String)](/ko/reference/data-types)) — 나열된 항목을 제외한 모든 역할 및/또는 사용자에게 ROW POLICY이 적용됩니다.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## 관련 항목
</div>

* [SHOW POLICIES](/ko/reference/statements/show#show-policies)