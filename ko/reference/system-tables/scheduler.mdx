---
description: '로컬 서버에 상주하는 스케줄링 노드의 정보와 상태를 담고 있는 시스템 테이블입니다.'
keywords: ['시스템 테이블', 'scheduler']
slug: /operations/system-tables/scheduler
title: 'system.scheduler'
doc_type: '참고'
---

import SystemTableCloud from '/snippets/ko/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## 설명
</div>

로컬 server에 있는 [스케줄링 노드](/ko/concepts/features/configuration/server-config/workload-scheduling#hierarchy)의 정보와 상태를 담고 있습니다.
이 table은 모니터링에 사용할 수 있습니다. 각 스케줄링 노드마다 하나의 행이 있습니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `resource` ([String](/ko/reference/data-types)) — 리소스 이름
* `path` ([String](/ko/reference/data-types)) — 이 리소스 스케줄링 계층 구조 내 스케줄링 노드의 경로
* `type` ([String](/ko/reference/data-types)) — 스케줄링 노드의 유형입니다.
* `weight` ([Float64](/ko/reference/data-types)) — 노드의 가중치로, `fair` 유형의 부모 노드에서 사용됩니다.
* `priority` ([Int64](/ko/reference/data-types)) — 노드의 우선순위로, &#39;priority&#39; 유형의 부모 노드에서 사용됩니다(값이 낮을수록 우선순위가 높습니다).
* `is_active` ([UInt8](/ko/reference/data-types)) — 이 노드가 현재 활성 상태인지 여부입니다. 즉, 큐에서 꺼낼 리소스 요청이 있고 제약 조건이 충족된 상태를 뜻합니다.
* `active_children` ([UInt64](/ko/reference/data-types)) — 활성 상태인 하위 노드 수입니다.
* `dequeued_requests` ([UInt64](/ko/reference/data-types)) — 이 노드에서 큐에서 꺼낸 리소스 요청의 총수입니다.
* `canceled_requests` ([UInt64](/ko/reference/data-types)) — 이 노드에서 취소된 리소스 요청의 총수입니다.
* `dequeued_cost` ([Int64](/ko/reference/data-types)) — 이 노드에서 큐에서 꺼낸 모든 요청 비용의 합계입니다(예: 바이트 단위 크기).
* `throughput` ([Float64](/ko/reference/data-types)) — 현재 평균 처리량입니다(초당 큐에서 꺼낸 비용).
* `canceled_cost` ([Int64](/ko/reference/data-types)) — 이 노드에서 취소된 모든 요청 비용의 합계입니다(예: 바이트 단위 크기).
* `busy_periods` ([UInt64](/ko/reference/data-types)) — 이 노드가 비활성화된 총 횟수입니다.
* `vruntime` ([Nullable(Float64)](/ko/reference/data-types)) — `fair` 노드의 하위 노드에만 해당합니다. SFQ 알고리즘이 다음에 처리할 하위 노드를 max-min 공정 방식으로 선택할 때 사용하는 노드의 가상 런타임입니다.
* `system_vruntime` ([Nullable(Float64)](/ko/reference/data-types)) — `fair` 노드에만 해당합니다. 마지막으로 처리된 리소스 요청의 `vruntime`을 나타내는 가상 런타임입니다. 하위 노드를 활성화할 때 `vruntime`의 새 값으로 사용됩니다.
* `queue_length` ([Nullable(UInt64)](/ko/reference/data-types)) — `fifo` 노드에만 해당합니다. 현재 큐에 있는 리소스 요청 수입니다.
* `queue_cost` ([Nullable(Int64)](/ko/reference/data-types)) — `fifo` 노드에만 해당합니다. 현재 큐에 있는 모든 요청 비용의 합계입니다(예: 바이트 단위 크기).
* `budget` ([Nullable(Int64)](/ko/reference/data-types)) — `fifo` 노드에만 해당합니다. 새 리소스 요청에 사용할 수 있는 &#39;비용 단위&#39; 수입니다. 리소스 요청의 예상 비용과 실제 비용이 다를 경우(예: 읽기/쓰기 실패 후) 나타날 수 있습니다.
* `is_satisfied` ([Nullable(UInt8)](/ko/reference/data-types)) — 제약 조건 노드에만 해당합니다(예: `inflight_limit`). 이 노드의 모든 제약 조건이 충족되면 `1`입니다.
* `inflight_requests` ([Nullable(Int64)](/ko/reference/data-types)) — `inflight_limit` 노드에만 해당합니다. 이 노드에서 큐에서 꺼냈으며 현재 처리 중인 리소스 요청 수입니다.
* `inflight_cost` ([Nullable(Int64)](/ko/reference/data-types)) — `inflight_limit` 노드에만 해당합니다. 이 노드에서 큐에서 꺼냈으며 현재 처리 중인 모든 리소스 요청 비용의 합계입니다(예: 바이트).
* `max_requests` ([Nullable(Int64)](/ko/reference/data-types)) — `inflight_limit` 노드에만 해당합니다. 제약 조건 위반을 유발하는 inflight&#95;requests의 상한입니다.
* `max_cost` ([Nullable(Int64)](/ko/reference/data-types)) — `inflight_limit` 노드에만 해당합니다. 제약 조건 위반을 유발하는 inflight&#95;cost의 상한입니다.
* `max_speed` ([Nullable(Float64)](/ko/reference/data-types)) — `bandwidth_limit` 노드에만 해당합니다. 초당 토큰 수 기준 대역폭 상한입니다.
* `max_burst` ([Nullable(Float64)](/ko/reference/data-types)) — `bandwidth_limit` 노드에만 해당합니다. 토큰 버킷 스로틀러에서 사용할 수 있는 토큰의 상한입니다.
* `throttling_us` ([Nullable(Int64)](/ko/reference/data-types)) — `bandwidth_limit` 노드에만 해당합니다. 이 노드가 스로틀링 상태였던 총 마이크로초 수입니다.
* `tokens` ([Nullable(Float64)](/ko/reference/data-types)) — `bandwidth_limit` 노드에만 해당합니다. 현재 토큰 버킷 스로틀러에서 사용할 수 있는 토큰 수입니다.

{/*AUTOGENERATED_END*/}

예시:

```sql
SELECT *
FROM system.scheduler
WHERE resource = 'network_read' AND path = '/prio/fair/prod'
FORMAT Vertical
```

```text
Row 1:
──────
resource:          network_read
path:              /prio/fair/prod
type:              fifo
weight:            5
priority:          0
is_active:         0
active_children:   0
dequeued_requests: 67
canceled_requests: 0
dequeued_cost:     4692272
canceled_cost:     0
busy_periods:      63
vruntime:          938454.1999999989
system_vruntime:   ᴺᵁᴸᴸ
queue_length:      0
queue_cost:        0
budget:            -60524
is_satisfied:      ᴺᵁᴸᴸ
inflight_requests: ᴺᵁᴸᴸ
inflight_cost:     ᴺᵁᴸᴸ
max_requests:      ᴺᵁᴸᴸ
max_cost:          ᴺᵁᴸᴸ
max_speed:         ᴺᵁᴸᴸ
max_burst:         ᴺᵁᴸᴸ
throttling_us:     ᴺᵁᴸᴸ
tokens:            ᴺᵁᴸᴸ
```