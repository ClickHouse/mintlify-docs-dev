# Mintlify automatically ignores these files and directories:
# .git, .github, .claude, .agents, .idea, node_modules,
# README.md, LICENSE.md, CHANGELOG.md, CONTRIBUTING.md

# Draft content
drafts/
*.draft.mdx

# Vale test fixtures (intentionally invalid, used by linter tests)
scripts/vale/test/test_headings_must_fail.md

# Skill scaffolds (internal docs for Claude Code, not user-facing pages)
.mintlify/

# Converted VLDB paper — uses ~100 inline <a id="page-N-M"> figure/citation
# anchors. They work in browser, but mint broken-links --check-anchors only
# recognizes heading slugs. Skip this file to avoid checker noise.
core/concepts/core-concepts/academic-overview.mdx
