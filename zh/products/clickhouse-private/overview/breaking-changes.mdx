---
hidden: true
title: "破坏性变更"
slug: "docs/products/clickhouse-private/breaking-changes"
---

<div id="helmonprem-clickhouse-cluster-version-11210">
  ### `helm/onprem-clickhouse-cluster` version `1.1.210`
</div>

从 `1.1.210` 版本的 chart 开始，默认已包含新的配置约束。如果您使用的 ClickHouse server 版本 **低于 25.10**，则必须在服务器配置中添加以下内容，以排除这些约束：

```yaml
server:
  excludeConfigConstraints:
    - allow_experimental_qbit_type
    - rewrite_in_to_join
    - allow_experimental_alias_table_engine
```

如果没有此配置，ClickHouse 将无法启动，因为 25.10 之前的版本不识别这些设置。