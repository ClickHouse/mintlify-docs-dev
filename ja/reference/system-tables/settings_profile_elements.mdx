---
description: '設定プロファイルの内容、つまり設定の適用先である制約、ロール、ユーザー、および親設定プロファイルを示すシステムテーブルです。'
keywords: ['システムテーブル', 'settings_profile_elements']
slug: /operations/system-tables/settings_profile_elements
title: 'system.settings_profile_elements'
doc_type: 'reference'
---

<div id="description">
  ## 説明
</div>

設定プロファイルの内容を示します。

* 制約。
* この設定が適用されるロールとユーザー。
* 親設定プロファイル。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/}

* `profile_name` ([Nullable(String)](/ja/reference/data-types)) — 設定プロファイル名。
* `user_name` ([Nullable(String)](/ja/reference/data-types)) — ユーザー名。
* `role_name` ([Nullable(String)](/ja/reference/data-types)) — ロール名。
* `index` ([UInt64](/ja/reference/data-types)) — 設定プロファイル要素の連続番号。
* `setting_name` ([Nullable(String)](/ja/reference/data-types)) — 設定名。
* `value` ([Nullable(String)](/ja/reference/data-types)) — 設定値。
* `min` ([Nullable(String)](/ja/reference/data-types)) — 設定の最小値。設定されていない場合は NULL。
* `max` ([Nullable(String)](/ja/reference/data-types)) — 設定の最大値。設定されていない場合は NULL。
* `writability` ([Nullable(Enum8(&#39;WRITABLE&#39; = 0, &#39;CONST&#39; = 1, &#39;CHANGEABLE&#95;IN&#95;READONLY&#39; = 2))](/ja/reference/data-types)) — 設定を変更できるかどうかを示すプロパティ。
* `inherit_profile` ([Nullable(String)](/ja/reference/data-types)) — この設定プロファイルの親プロファイル。設定されていない場合は NULL。設定プロファイルは、親プロファイルから各設定の値と制約 (min、max、readonly) をすべて継承します。

{/*AUTOGENERATED_END*/}