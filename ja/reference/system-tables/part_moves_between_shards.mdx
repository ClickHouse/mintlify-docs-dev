---
description: '現在、分片間で移動中のパーツと、その進捗に関する情報が含まれています。'
keywords: ['システムテーブル', 'part_moves_between_shards']
slug: /operations/system-tables/part_moves_between_shards
title: 'system.part_moves_between_shards'
doc_type: 'reference'
---

<div id="description">
  ## 説明
</div>

現在、分片間で移動中のパーツと、その進捗に関する情報が含まれます。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/}

* `database` ([String](/ja/reference/data-types)) — 移動が実行されるデータベースの名前。
* `table` ([String](/ja/reference/data-types)) — 移動が実行されるテーブルの名前。
* `task_name` ([String](/ja/reference/data-types)) — 移動タスクの名前。
* `task_uuid` ([UUID](/ja/reference/data-types)) — 移動タスクの識別子。
* `create_time` ([DateTime](/ja/reference/data-types)) — タスクが作成された時刻。
* `part_name` ([String](/ja/reference/data-types)) — 移動処理中のパート名。
* `part_uuid` ([UUID](/ja/reference/data-types)) — 移動処理中のパートの UUID。
* `to_shard` ([String](/ja/reference/data-types)) — 宛先分片の名前。
* `dst_part_name` ([String](/ja/reference/data-types)) — 移動後の結果パート名。
* `update_time` ([DateTime](/ja/reference/data-types)) — 最後に更新された時刻。
* `state` ([String](/ja/reference/data-types)) — 移動の現在の状態。
* `rollback` ([UInt8](/ja/reference/data-types)) — 操作がロールバックされたかどうかを示すフラグ。
* `num_tries` ([UInt32](/ja/reference/data-types)) — 操作を完了するまでの試行回数。
* `last_exception` ([String](/ja/reference/data-types)) — 直近の例外名 (存在する場合) 。

{/*AUTOGENERATED_END*/}