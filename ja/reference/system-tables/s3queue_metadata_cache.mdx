---
description: 'S3Queue メタデータのインメモリ状態と、ファイルごとの現在処理中の行が含まれます。'
keywords: ['システムテーブル', 's3queue_metadata_cache']
slug: /operations/system-tables/s3queue_metadata_cache
title: 'system.s3queue_metadata_cache'
doc_type: 'reference'
---

<div id="description">
  ## 説明
</div>

S3Queue メタデータのインメモリ状態と、ファイルごとの現在処理中の行が含まれます。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/}

* `zookeeper_path` ([String](/ja/reference/data-types)) — zookeeper 内のメタデータへのパス
* `file_path` ([String](/ja/reference/data-types)) — 処理対象ファイルのパス
* `file_name` ([String](/ja/reference/data-types)) — 処理対象ファイルの名前
* `rows_processed` ([UInt64](/ja/reference/data-types)) — 現在処理中の行数
* `status` ([String](/ja/reference/data-types)) — 処理状態: Processed、Processing、Failed
* `processing_start_time` ([Nullable(DateTime)](/ja/reference/data-types)) — ファイルの処理が開始された時刻
* `processing_end_time` ([Nullable(DateTime)](/ja/reference/data-types)) — ファイルの処理が終了した時刻
* `exception` ([String](/ja/reference/data-types)) — 処理中に発生した例外

{/*AUTOGENERATED_END*/}