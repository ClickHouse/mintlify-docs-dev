---
description: 'Системная таблица со списком учётных записей пользователей, настроенных на сервере.'
keywords: ['системная таблица', 'пользователи']
slug: /operations/system-tables/users
title: 'system.users'
doc_type: 'reference'
---

<div id="description">
  ## Описание
</div>

Содержит список [учётных записей пользователей](/ru/concepts/features/security/access-rights#user-account-management), настроенных на сервере.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ru/reference/data-types)) — Имя пользователя.
* `id` ([UUID](/ru/reference/data-types)) — Идентификатор пользователя.
* `storage` ([String](/ru/reference/data-types)) — Путь к хранилищу пользователей. Настраивается в параметре access&#95;control&#95;path.
* `auth_type` ([Array(Enum8(&#39;no&#95;password&#39; = 0, &#39;plaintext&#95;password&#39; = 1, &#39;sha256&#95;password&#39; = 2, &#39;double&#95;sha1&#95;password&#39; = 3, &#39;ldap&#39; = 4, &#39;kerberos&#39; = 5, &#39;ssl&#95;certificate&#39; = 6, &#39;bcrypt&#95;password&#39; = 7, &#39;ssh&#95;key&#39; = 8, &#39;http&#39; = 9, &#39;jwt&#39; = 10, &#39;scram&#95;sha256&#95;password&#39; = 11, &#39;no&#95;authentication&#39; = 12))](/ru/reference/data-types)) — Показывает типы аутентификации. Существует несколько способов идентификации пользователя: без пароля, с паролем в открытом виде, с паролем, закодированным с помощью SHA256, с паролем, закодированным с помощью двойного SHA-1, или с паролем, закодированным с помощью bcrypt.
* `auth_params` ([Array(String)](/ru/reference/data-types)) — Параметры аутентификации в формате JSON в зависимости от `auth_type`.
* `valid_until` ([Array(DateTime)](/ru/reference/data-types)) — Дата и время истечения срока действия учетных данных пользователя.
* `host_ip` ([Array(String)](/ru/reference/data-types)) — IP-адреса хостов, которым разрешено подключаться к серверу ClickHouse.
* `host_names` ([Array(String)](/ru/reference/data-types)) — Имена хостов, которым разрешено подключаться к серверу ClickHouse.
* `host_names_regexp` ([Array(String)](/ru/reference/data-types)) — Регулярное выражение для имен хостов, которым разрешено подключаться к серверу ClickHouse.
* `host_names_like` ([Array(String)](/ru/reference/data-types)) — Имена хостов, которым разрешено подключаться к серверу ClickHouse, заданные с помощью предиката LIKE.
* `default_roles_all` ([UInt8](/ru/reference/data-types)) — Показывает, что все выданные роли установлены для пользователя по умолчанию.
* `default_roles_list` ([Array(String)](/ru/reference/data-types)) — Список выданных ролей, используемых по умолчанию.
* `default_roles_except` ([Array(String)](/ru/reference/data-types)) — Все выданные роли установлены по умолчанию, кроме перечисленных.
* `grantees_any` ([UInt8](/ru/reference/data-types)) — Флаг, указывающий, может ли пользователь с любой опцией grant выдавать ее кому угодно.
* `grantees_list` ([Array(String)](/ru/reference/data-types)) — Список пользователей или ролей, которым этот пользователь может выдавать опции.
* `grantees_except` ([Array(String)](/ru/reference/data-types)) — Список пользователей или ролей, которым этому пользователю запрещено выдавать опции grant.
* `default_database` ([String](/ru/reference/data-types)) — Имя database по умолчанию для этого пользователя.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## См. также
</div>

* [SHOW USERS](/ru/reference/statements/show#show-users)