---
description: 'Системная таблица, содержащая сведения об отсоединённых частях таблиц
  MergeTree'
keywords: ['system table', 'detached_parts']
slug: /operations/system-tables/detached_parts
title: 'system.detached_parts'
doc_type: 'reference'
---

<div id="purpose">
  ## Описание
</div>

Содержит информацию об отсоединённых частях таблиц [MergeTree](/ru/reference/engines/table-engines/mergetree-family/mergetree). Столбец `reason` указывает, по какой причине часть была отсоединена.

Для частей, отсоединённых пользователем, причина не указана. Такие части можно подключить командой [ALTER TABLE ATTACH PARTITION|PART](/ru/reference/statements/alter/partition#attach-partitionpart).

Описание остальных столбцов см. в [system.parts](/ru/reference/system-tables/parts).

Если имя части некорректно, значения некоторых столбцов могут быть `NULL`. Такие части можно удалить командой [ALTER TABLE DROP DETACHED PART](/ru/reference/statements/alter/partition#drop-detached-partitionpart).

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `database` ([String](/ru/reference/data-types)) — Имя базы данных, к которой относится эта часть.
* `table` ([String](/ru/reference/data-types)) — Имя таблицы, к которой относится эта часть.
* `partition_id` ([Nullable(String)](/ru/reference/data-types)) — Идентификатор партиции, к которой относится эта часть.
* `name` ([String](/ru/reference/data-types)) — Имя части.
* `bytes_on_disk` ([UInt64](/ru/reference/data-types)) — Общий размер всех файлов части данных в байтах.
* `modification_time` ([DateTime](/ru/reference/data-types)) — Время изменения каталога с частью данных. Обычно это соответствует времени, когда часть была отсоединена.
* `disk` ([String](/ru/reference/data-types)) — Имя диска, на котором хранится эта часть данных.
* `path` ([String](/ru/reference/data-types)) — Путь на диске к файлу этой части данных.
* `reason` ([Nullable(String)](/ru/reference/data-types)) — Объяснение, почему эта часть была отсоединена.
* `min_block_number` ([Nullable(Int64)](/ru/reference/data-types)) — Минимальный номер частей данных, из которых после слияния состоит текущая часть.
* `max_block_number` ([Nullable(Int64)](/ru/reference/data-types)) — Максимальный номер частей данных, из которых после слияния состоит текущая часть.
* `level` ([Nullable(UInt32)](/ru/reference/data-types)) — Глубина дерева слияния. Ноль означает, что текущая часть была создана вставкой, а не слиянием других частей.

{/*AUTOGENERATED_END*/}