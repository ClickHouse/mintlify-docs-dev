---
description: 'Системная таблица, содержащая данные об использовании квот всеми пользователями.'
keywords: ['системная таблица', 'quotas_usage', 'квота']
slug: /operations/system-tables/quotas_usage
title: 'system.quotas_usage'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/ru/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Описание
</div>

Использование квот всеми пользователями.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `quota_name` ([String](/ru/reference/data-types)) — Имя квоты.
* `quota_key` ([String](/ru/reference/data-types)) — Значение ключа.
* `is_current` ([UInt8](/ru/reference/data-types)) — Использование квоты текущим пользователем.
* `start_time` ([Nullable(DateTime)](/ru/reference/data-types)) — Время начала расчета потребления ресурсов.
* `end_time` ([Nullable(DateTime)](/ru/reference/data-types)) — Время окончания расчета потребления ресурсов.
* `duration` ([Nullable(UInt32)](/ru/reference/data-types)) — Длительность временного интервала для расчета потребления ресурсов, в секундах.
* `queries` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее количество выполненных запросов.
* `max_queries` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество выполняемых запросов всех типов.
* `query_selects` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее количество выполненных запросов SELECT.
* `max_query_selects` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество выполняемых запросов SELECT.
* `query_inserts` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее количество выполненных запросов INSERT.
* `max_query_inserts` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество выполняемых запросов INSERT.
* `errors` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее количество запросов, завершившихся ошибкой.
* `max_errors` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество запросов, завершившихся ошибкой, в течение указанного периода времени.
* `result_rows` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее общее количество строк в результирующем наборе всех запросов за текущий период времени.
* `max_result_rows` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое общее количество строк в результирующем наборе всех запросов за указанный период времени.
* `result_bytes` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее общее количество байтов в результирующем наборе всех запросов за текущий период времени.
* `max_result_bytes` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое общее количество байтов в результирующем наборе всех запросов за указанный период времени.
* `read_rows` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее общее количество строк, прочитанных при выполнении всех запросов за текущий период времени.
* `max_read_rows` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество строк, считываемых при выполнении всех запросов за указанный период времени.
* `read_bytes` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее общее количество байтов, прочитанных при выполнении всех запросов за текущий период времени.
* `max_read_bytes` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество байтов, считываемых при выполнении всех запросов за указанный период времени.
* `execution_time` ([Nullable(Float64)](/ru/reference/data-types)) — Текущее суммарное время (в наносекундах), затраченное на выполнение запросов за текущий период времени
* `max_execution_time` ([Nullable(Float64)](/ru/reference/data-types)) — Максимально допустимое суммарное время (в наносекундах) на выполнение всех запросов за указанный период времени
* `written_bytes` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее общее количество байтов, записанных при выполнении всех запросов за текущий период времени.
* `max_written_bytes` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество байтов, записываемых при выполнении всех запросов за указанный период времени.
* `failed_sequential_authentications` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее количество последовательных неудачных попыток аутентификации за текущий период времени.
* `max_failed_sequential_authentications` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество последовательных неудачных попыток аутентификации за указанный период времени.
* `queries_per_normalized_hash` ([Nullable(UInt64)](/ru/reference/data-types)) — Текущее максимальное количество выполнений одного нормализованного запроса за текущий период времени.
* `max_queries_per_normalized_hash` ([Nullable(UInt64)](/ru/reference/data-types)) — Максимально допустимое количество выполнений одного нормализованного запроса за указанный период времени.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## См. также
</div>

* [SHOW QUOTA](/ru/reference/statements/show#show-quota))