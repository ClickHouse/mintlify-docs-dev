---
description: 'Системная таблица, содержащая фильтры для конкретной таблицы, а также
  список ролей и/или пользователей, к которым применяется эта ROW POLICY.'
keywords: ['системная таблица', 'row_policies']
slug: /operations/system-tables/row_policies
title: 'system.row_policies'
doc_type: 'reference'
---

<div id="description">
  ## Описание
</div>

Содержит фильтры для конкретной таблицы, а также список ролей и/или пользователей, для которых должна применяться эта ROW POLICY.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ru/reference/data-types)) — Имя ROW POLICY.
* `short_name` ([String](/ru/reference/data-types)) — Короткое имя ROW POLICY. Имена ROW POLICY являются составными, например: myfilter ON mydb.mytable. Здесь &#39;myfilter ON mydb.mytable&#39; — имя ROW POLICY, а &#39;myfilter&#39; — её короткое имя.
* `database` ([String](/ru/reference/data-types)) — Имя базы данных.
* `table` ([String](/ru/reference/data-types)) — Имя таблицы. Пусто, если политика задана для базы данных.
* `id` ([UUID](/ru/reference/data-types)) — ID ROW POLICY.
* `storage` ([String](/ru/reference/data-types)) — Имя каталога, в котором хранится ROW POLICY.
* `select_filter` ([Nullable(String)](/ru/reference/data-types)) — expression, используемое для фильтрации в запросах SELECT.
* `is_restrictive` ([UInt8](/ru/reference/data-types)) — Показывает, ограничивает ли ROW POLICY доступ к строкам. Значение: • 0 — ROW POLICY определена с помощью clause `AS PERMISSIVE`, • 1 — ROW POLICY определена с помощью clause `AS RESTRICTIVE`.
* `apply_to_all` ([UInt8](/ru/reference/data-types)) — Показывает, что ROW POLICY заданы для всех ролей и/или пользователей.
* `apply_to_list` ([Array(String)](/ru/reference/data-types)) — Список ролей и/или пользователей, к которым применяется ROW POLICY.
* `apply_to_except` ([Array(String)](/ru/reference/data-types)) — ROW POLICY применяется ко всем ролям и/или пользователям, кроме перечисленных.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## См. также
</div>

* [SHOW POLICIES](/ru/reference/statements/show#show-policies)