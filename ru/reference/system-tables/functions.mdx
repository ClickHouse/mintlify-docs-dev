---
description: 'Системная таблица со сведениями об обычных и агрегатных функциях.'
keywords: ['системная таблица', 'функции']
slug: /operations/system-tables/functions
title: 'system.functions'
doc_type: 'reference'
---

<div id="description">
  ## Описание
</div>

Содержит сведения об обычных и агрегатных функциях.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ru/reference/data-types)) — Имя функции.
* `is_aggregate` ([UInt8](/ru/reference/data-types)) — Является ли функция агрегатной.
* `case_insensitive` ([UInt8](/ru/reference/data-types)) — Можно ли использовать имя функции регистронезависимо.
* `alias_to` ([String](/ru/reference/data-types)) — Исходное имя функции, если имя функции является псевдонимом.
* `create_query` ([String](/ru/reference/data-types)) — Устарело.
* `origin` ([Enum8(&#39;System&#39; = 0, &#39;SQLUserDefined&#39; = 1, &#39;ExecutableUserDefined&#39; = 2, &#39;WasmUserDefined&#39; = 3)](/ru/reference/data-types)) — Устарело.
* `description` ([String](/ru/reference/data-types)) — Общее описание того, что делает функция.
* `syntax` ([String](/ru/reference/data-types)) — Сигнатура функции.
* `arguments` ([String](/ru/reference/data-types)) — Аргументы функции.
* `parameters` ([String](/ru/reference/data-types)) — Параметры функции (только для агрегатной функции).
* `returned_value` ([String](/ru/reference/data-types)) — Что возвращает функция.
* `examples` ([String](/ru/reference/data-types)) — Пример использования.
* `introduced_in` ([String](/ru/reference/data-types)) — Версия ClickHouse, в которой функция впервые появилась.
* `categories` ([String](/ru/reference/data-types)) — Категория функции.
* `deterministic` ([Nullable(UInt8)](/ru/reference/data-types)) — Возвращает ли функция один и тот же результат для одинаковых аргументов. NULL, если это неизвестно (например, для агрегатных или пользовательских функций).
* `higher_order` ([Nullable(UInt8)](/ru/reference/data-types)) — Является ли функция функцией высшего порядка, то есть принимает ли она как минимум одно лямбда-выражение в качестве аргумента (например, arrayMap, arrayFilter, mapApply). NULL, если это неизвестно.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Пример
</div>

```sql title="Query"
 SELECT name, is_aggregate, deterministic, case_insensitive, alias_to FROM system.functions LIMIT 5;
```

```text title="Response"
┌─name─────────────────────┬─is_aggregate─┬─deterministic─┬─case_insensitive─┬─alias_to─┐
│ BLAKE3                   │            0 │                1 │                0 │          │
│ sipHash128Reference      │            0 │                1 │                0 │          │
│ mapExtractKeyLike        │            0 │                1 │                0 │          │
│ sipHash128ReferenceKeyed │            0 │                1 │                0 │          │
│ mapPartialSort           │            0 │                1 │                0 │          │
└──────────────────────────┴──────────────┴──────────────────┴──────────────────┴──────────┘

5 rows in set. Elapsed: 0.002 sec.
```