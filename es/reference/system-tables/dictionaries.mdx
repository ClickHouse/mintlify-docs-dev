---
description: 'Tabla del sistema con información sobre diccionarios'
keywords: ['tabla del sistema', 'diccionarios']
slug: /operations/system-tables/dictionaries
title: 'system.dictionaries'
doc_type: 'referencia'
---

import SystemTableCloud from '/snippets/es/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Descripción
</div>

Contiene información sobre [diccionarios](/es/reference/statements/create/dictionary).

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/}

* `database` ([String](/es/reference/data-types)) — Nombre de la base de datos que contiene el diccionario creado por la consulta DDL. Cadena vacía para los demás diccionarios.
* `name` ([String](/es/reference/data-types)) — Nombre del diccionario.
* `uuid` ([UUID](/es/reference/data-types)) — UUID del diccionario.
* `status` ([Enum8(&#39;NOT&#95;LOADED&#39; = 0, &#39;LOADED&#39; = 1, &#39;FAILED&#39; = 2, &#39;LOADING&#39; = 3, &#39;FAILED&#95;AND&#95;RELOADING&#39; = 4, &#39;LOADED&#95;AND&#95;RELOADING&#39; = 5, &#39;NOT&#95;EXIST&#39; = 6)](/es/reference/data-types)) — Estado del diccionario. Posibles valores:
  * **NOT&#95;LOADED** — El diccionario no se cargó porque no se utilizó
  * **LOADED** — El diccionario se cargó correctamente
  * **FAILED** — No se pudo cargar el diccionario debido a un error
  * **LOADING** — El diccionario se está cargando en este momento
  * **LOADED&#95;AND&#95;RELOADING** — El diccionario se cargó correctamente
  * **and is being reloaded right now (frequent reasons: SYSTEM RELOAD DICTIONARY query** —
  * **tiempo de espera agotado** —
  * **la configuración del diccionario ha cambiado)** —
  * **FAILED&#95;AND&#95;RELOADING** — No se pudo cargar el diccionario debido a un error y se está cargando ahora.
* `origin` ([String](/es/reference/data-types)) — Ruta al archivo de configuración que describe el diccionario.
* `type` ([String](/es/reference/data-types)) — Tipo de asignación del diccionario. Almacenamiento de diccionarios en memoria.
* `key.names` ([Array(String)](/es/reference/data-types)) — Array de nombres de clave proporcionados por el diccionario.
* `key.types` ([Array(String)](/es/reference/data-types)) — Array correspondiente de tipos de clave proporcionados por el diccionario.
* `attribute.names` ([Array(String)](/es/reference/data-types)) — Array de nombres de atributos proporcionados por el diccionario.
* `attribute.types` ([Array(String)](/es/reference/data-types)) — Array correspondiente de tipos de atributos proporcionados por el diccionario.
* `bytes_allocated` ([UInt64](/es/reference/data-types)) — Cantidad de RAM asignada al diccionario.
* `hierarchical_index_bytes_allocated` ([UInt64](/es/reference/data-types)) — Cantidad de RAM asignada al índice jerárquico.
* `query_count` ([UInt64](/es/reference/data-types)) — Número de consultas desde que se cargó el diccionario o desde el último reinicio correcto.
* `hit_rate` ([Float64](/es/reference/data-types)) — En los diccionarios de caché, el porcentaje de usos en los que el valor estaba en la caché.
* `found_rate` ([Float64](/es/reference/data-types)) — Porcentaje de usos en los que se encontró el valor.
* `element_count` ([UInt64](/es/reference/data-types)) — Número de elementos almacenados en el diccionario.
* `load_factor` ([Float64](/es/reference/data-types)) — Porcentaje de ocupación del diccionario (en un diccionario hash, el porcentaje de ocupación de la tabla hash).
* `source` ([String](/es/reference/data-types)) — Texto que describe la fuente de datos del diccionario.
* `lifetime_min` ([UInt64](/es/reference/data-types)) — Tiempo de vida mínimo del diccionario en memoria, tras el cual ClickHouse intenta recargar el diccionario (si `invalidate_query` está configurado, solo si ha cambiado). Se establece en segundos.
* `lifetime_max` ([UInt64](/es/reference/data-types)) — Tiempo de vida máximo del diccionario en memoria, tras el cual ClickHouse intenta recargar el diccionario (si `invalidate_query` está configurado, solo si ha cambiado). Se establece en segundos.
* `loading_start_time` ([DateTime](/es/reference/data-types)) — Hora de inicio de la carga del diccionario.
* `last_successful_update_time` ([DateTime](/es/reference/data-types)) — Hora de finalización de la carga o actualización del diccionario. Ayuda a supervisar algunos problemas con las fuentes de diccionario e investigar sus causas.
* `error_count` ([UInt64](/es/reference/data-types)) — Número de errores desde la última carga correcta. Ayuda a supervisar algunos problemas con las fuentes de diccionario e investigar sus causas.
* `loading_duration` ([Float32](/es/reference/data-types)) — Duración de la carga de un diccionario.
* `last_exception` ([String](/es/reference/data-types)) — Texto del error que se produce al crear o recargar el diccionario si no pudo crearse.
* `comment` ([String](/es/reference/data-types)) — Texto del comentario del diccionario.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Ejemplo
</div>

Configure el diccionario:

```sql
CREATE DICTIONARY dictionary_with_comment
(
    id UInt64,
    value String
)
PRIMARY KEY id
SOURCE(CLICKHOUSE(HOST 'localhost' PORT tcpPort() TABLE 'source_table'))
LAYOUT(FLAT())
LIFETIME(MIN 0 MAX 1000)
COMMENT 'The temporary dictionary';
```

Asegúrese de que el diccionario esté cargado.

```sql
SELECT * FROM system.dictionaries LIMIT 1 FORMAT Vertical;
```

```text
Row 1:
──────
database:                    default
name:                        dictionary_with_comment
uuid:                        4654d460-0d03-433a-8654-d4600d03d33a
status:                      NOT_LOADED
origin:                      4654d460-0d03-433a-8654-d4600d03d33a
type:
key.names:                   ['id']
key.types:                   ['UInt64']
attribute.names:             ['value']
attribute.types:             ['String']
bytes_allocated:             0
query_count:                 0
hit_rate:                    0
found_rate:                  0
element_count:               0
load_factor:                 0
source:
lifetime_min:                0
lifetime_max:                0
loading_start_time:          1970-01-01 00:00:00
last_successful_update_time: 1970-01-01 00:00:00
loading_duration:            0
last_exception:
comment:                     The temporary dictionary
```