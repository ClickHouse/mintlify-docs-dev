---
description: 'Tabla del sistema con información sobre las fusiones y las mutaciones de partes
  actualmente en proceso en tablas de la familia MergeTree.'
keywords: ['tabla del sistema', 'fusiones']
slug: /operations/system-tables/merges
title: 'system.merges'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/es/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Descripción
</div>

Contiene información sobre las fusiones y las mutaciones de partes actualmente en curso en las tablas de la familia MergeTree.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/}

* `database` ([String](/es/reference/data-types)) — El nombre de la base de datos en la que se encuentra la tabla.
* `table` ([String](/es/reference/data-types)) — Nombre de la tabla.
* `elapsed` ([Float64](/es/reference/data-types)) — El tiempo transcurrido (en segundos) desde que comenzó la fusión.
* `progress` ([Float64](/es/reference/data-types)) — El porcentaje de trabajo completado, de 0 a 1.
* `num_parts` ([UInt64](/es/reference/data-types)) — El número de partes que se van a fusionar.
* `source_part_names` ([Array(String)](/es/reference/data-types)) — La lista de nombres de las partes de origen.
* `result_part_name` ([String](/es/reference/data-types)) — El nombre de la parte que se formará como resultado de la fusión.
* `source_part_paths` ([Array(String)](/es/reference/data-types)) — La lista de rutas de cada parte de origen.
* `result_part_path` ([String](/es/reference/data-types)) — La ruta de la parte que se formará como resultado de la fusión.
* `partition_id` ([String](/es/reference/data-types)) — El identificador de la partición donde se está realizando la fusión.
* `partition` ([String](/es/reference/data-types)) — El nombre de la partición
* `is_mutation` ([UInt8](/es/reference/data-types)) — 1 si este proceso es una mutación de parte.
* `total_size_bytes_compressed` ([UInt64](/es/reference/data-types)) — El tamaño total de los datos comprimidos en los fragmentos fusionados.
* `total_size_bytes_uncompressed` ([UInt64](/es/reference/data-types)) — El tamaño total de los datos sin comprimir en los fragmentos fusionados.
* `total_size_marks` ([UInt64](/es/reference/data-types)) — El número total de marks en las partes fusionadas.
* `bytes_read_uncompressed` ([UInt64](/es/reference/data-types)) — Número de bytes leídos, sin comprimir.
* `rows_read` ([UInt64](/es/reference/data-types)) — Número de filas leídas.
* `bytes_written_uncompressed` ([UInt64](/es/reference/data-types)) — Número de bytes escritos, sin comprimir.
* `rows_written` ([UInt64](/es/reference/data-types)) — Número de filas escritas.
* `columns_written` ([UInt64](/es/reference/data-types)) — Número de columnas escritas (para el algoritmo de fusión vertical).
* `memory_usage` ([UInt64](/es/reference/data-types)) — Consumo de memoria del proceso de fusión.
* `thread_id` ([UInt64](/es/reference/data-types)) — ID del hilo del proceso de fusión.
* `merge_type` ([String](/es/reference/data-types)) — El tipo de la fusión actual. Vacío si es una mutación.
* `merge_algorithm` ([String](/es/reference/data-types)) — El algoritmo utilizado en la fusión actual. Vacío si es una mutación.

{/*AUTOGENERATED_END*/}