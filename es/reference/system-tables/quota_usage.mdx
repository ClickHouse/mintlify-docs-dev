---
description: 'Tabla del sistema que contiene información sobre el uso de la QUOTA por parte del usuario actual,
  como cuánto de la QUOTA se ha utilizado y cuánto queda.'
keywords: ['tabla del sistema', 'quota_usage']
slug: /operations/system-tables/quota_usage
title: 'system.quota_usage'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/es/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Descripción
</div>

Uso de la QUOTA del usuario actual: cuánto se ha usado y cuánto queda.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/}

* `quota_name` ([String](/es/reference/data-types)) — Nombre de la QUOTA.
* `quota_key` ([String](/es/reference/data-types)) — Valor de la clave.
* `start_time` ([Nullable(DateTime)](/es/reference/data-types)) — Hora de inicio para calcular el consumo de recursos.
* `end_time` ([Nullable(DateTime)](/es/reference/data-types)) — Hora de finalización para calcular el consumo de recursos.
* `duration` ([Nullable(UInt32)](/es/reference/data-types)) — Duración del intervalo de tiempo para calcular el consumo de recursos, en segundos.
* `queries` ([Nullable(UInt64)](/es/reference/data-types)) — Número actual de consultas ejecutadas.
* `max_queries` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de consultas de cualquier tipo que se pueden ejecutar.
* `query_selects` ([Nullable(UInt64)](/es/reference/data-types)) — Número actual de consultas SELECT ejecutadas.
* `max_query_selects` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de consultas SELECT que se pueden ejecutar.
* `query_inserts` ([Nullable(UInt64)](/es/reference/data-types)) — Número actual de consultas INSERT ejecutadas.
* `max_query_inserts` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de consultas INSERT que se pueden ejecutar.
* `errors` ([Nullable(UInt64)](/es/reference/data-types)) — Número actual de consultas que han dado error.
* `max_errors` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de consultas que pueden dar error dentro del período de tiempo especificado.
* `result_rows` ([Nullable(UInt64)](/es/reference/data-types)) — Número total actual de filas en el conjunto de resultados de todas las consultas dentro del período de tiempo actual.
* `max_result_rows` ([Nullable(UInt64)](/es/reference/data-types)) — Número total máximo permitido de filas en el conjunto de resultados de todas las consultas dentro del período de tiempo especificado.
* `result_bytes` ([Nullable(UInt64)](/es/reference/data-types)) — Número total actual de bytes en el conjunto de resultados de todas las consultas dentro del período de tiempo actual.
* `max_result_bytes` ([Nullable(UInt64)](/es/reference/data-types)) — Número total máximo permitido de bytes en el conjunto de resultados de todas las consultas dentro del período de tiempo especificado.
* `read_rows` ([Nullable(UInt64)](/es/reference/data-types)) — Número total actual de filas leídas durante la ejecución de todas las consultas dentro del período de tiempo actual.
* `max_read_rows` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de filas que se pueden leer durante la ejecución de todas las consultas dentro del período de tiempo especificado.
* `read_bytes` ([Nullable(UInt64)](/es/reference/data-types)) — Número total actual de bytes leídos durante la ejecución de todas las consultas dentro del período de tiempo actual.
* `max_read_bytes` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de bytes que se pueden leer durante la ejecución de todas las consultas dentro del período de tiempo especificado.
* `execution_time` ([Nullable(Float64)](/es/reference/data-types)) — Cantidad total actual de tiempo (en nanosegundos) empleada en ejecutar consultas dentro del período de tiempo actual
* `max_execution_time` ([Nullable(Float64)](/es/reference/data-types)) — Cantidad máxima de tiempo (en nanosegundos) permitida para ejecutar todas las consultas dentro del período de tiempo especificado
* `written_bytes` ([Nullable(UInt64)](/es/reference/data-types)) — Número total actual de bytes escritos durante la ejecución de todas las consultas dentro del período de tiempo actual.
* `max_written_bytes` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de bytes que se pueden escribir durante la ejecución de todas las consultas dentro del período de tiempo especificado.
* `failed_sequential_authentications` ([Nullable(UInt64)](/es/reference/data-types)) — Número actual de fallos consecutivos de autenticación dentro del período de tiempo actual.
* `max_failed_sequential_authentications` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de fallos consecutivos de autenticación dentro del período de tiempo especificado.
* `queries_per_normalized_hash` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo actual de ejecuciones de una misma consulta normalizada dentro del período de tiempo actual.
* `max_queries_per_normalized_hash` ([Nullable(UInt64)](/es/reference/data-types)) — Número máximo permitido de ejecuciones de una misma consulta normalizada dentro del período de tiempo especificado.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## Véase también
</div>

* [SHOW QUOTA](/es/reference/statements/show#show-quota))