---
description: 'Tabla del sistema que describe el contenido del perfil de configuración: restricciones,
  roles y usuarios a los que se aplica la configuración, y perfiles de configuración padre.'
keywords: ['tabla del sistema', 'settings_profile_elements']
slug: /operations/system-tables/settings_profile_elements
title: 'system.settings_profile_elements'
doc_type: 'referencia'
---

<div id="description">
  ## Descripción
</div>

Describe el contenido del perfil de configuración:

* Restricciones.
* Roles y usuarios a los que se aplica la configuración.
* Perfiles de configuración superiores.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/}

* `profile_name` ([Nullable(String)](/es/reference/data-types)) — Nombre del perfil de configuración.
* `user_name` ([Nullable(String)](/es/reference/data-types)) — Nombre de usuario.
* `role_name` ([Nullable(String)](/es/reference/data-types)) — Nombre del rol.
* `index` ([UInt64](/es/reference/data-types)) — Número secuencial del elemento del perfil de configuración.
* `setting_name` ([Nullable(String)](/es/reference/data-types)) — Nombre de la configuración.
* `value` ([Nullable(String)](/es/reference/data-types)) — Valor de la configuración.
* `min` ([Nullable(String)](/es/reference/data-types)) — Valor mínimo de la configuración. NULL si no está definido.
* `max` ([Nullable(String)](/es/reference/data-types)) — Valor máximo de la configuración. NULL si no está definido.
* `writability` ([Nullable(Enum8(&#39;WRITABLE&#39; = 0, &#39;CONST&#39; = 1, &#39;CHANGEABLE&#95;IN&#95;READONLY&#39; = 2))](/es/reference/data-types)) — Propiedad que indica si una configuración se puede modificar o no.
* `inherit_profile` ([Nullable(String)](/es/reference/data-types)) — Perfil padre de este perfil de configuración. NULL si no está definido. El perfil de configuración heredará todos los valores y restricciones (min, max, readonly) de sus perfiles padre.

{/*AUTOGENERATED_END*/}