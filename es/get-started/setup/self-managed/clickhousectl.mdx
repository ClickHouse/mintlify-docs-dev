---
description: 'Instala y gestiona versiones locales de ClickHouse con clickhousectl, la ClickHouse CLI'
keywords: ['ClickHouse', 'instalar', 'clickhousectl', 'CLI']
sidebarTitle: 'clickhousectl (CLI)'
slug: /install/clickhousectl
title: 'Instalar con la ClickHouse CLI'
doc_type: 'guide'
---

<Info>
  **Usuarios de Windows**

  ClickHouse se ejecuta de forma nativa en Linux y macOS. En Windows, sigue estos pasos dentro del
  [Subsistema de Windows para Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/about).
</Info>

<div id="install-clickhouse-using-the-clickhouse-cli">
  # Instalar ClickHouse con ClickHouse CLI
</div>

ClickHouse CLI (`clickhousectl`) le ayuda a instalar y gestionar versiones locales de ClickHouse,
iniciar servidores y ejecutar consultas.

<Steps>
  <Step>
    ## Instalar ClickHouse CLI

    ```bash
    curl https://clickhouse.com/cli | sh
    ```

    También se crea automáticamente el alias `chctl` para mayor comodidad.
  </Step>

  <Step>
    ## Instalar ClickHouse

    Instale la última versión estable de ClickHouse:

    ```bash
    clickhousectl local install stable
    ```

    También puede instalar una versión específica:

    ```bash
    clickhousectl local install lts             # Última versión LTS
    clickhousectl local install 25.6            # Última versión 25.6.x.x
    clickhousectl local install 25.6.1.1        # Versión exacta
    ```
  </Step>

  <Step>
    ## Iniciar clickhouse-server

    ```bash
    clickhousectl local server start
    ```

    El servidor se ejecuta en segundo plano. Para comprobar que está en ejecución:

    ```bash
    clickhousectl local server list
    ```
  </Step>

  <Step>
    ## Iniciar clickhouse-client

    ```bash
    clickhousectl local client
    ```

    Verá algo como esto:

    ```response
    ClickHouse client version 24.5.1.117 (official build).
    Connecting to localhost:9000 as user default.
    Connected to ClickHouse server version 24.5.1.

    local-host :)
    ```

    Ya está listo para empezar a enviar comandos SQL a ClickHouse.

    <Tip>
      La [Quick Start](/es/get-started/setup/install) le guía por los pasos para crear tablas e insertar datos.
    </Tip>
  </Step>
</Steps>
